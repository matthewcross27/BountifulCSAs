// Single-farm demo: the dashboard has no auth/multi-tenant routing yet (see AGENTS.md),
// so every payments API scopes to this one seeded farm ("Jamie · Ridgefoot" in the UI).
export const DEMO_FARM_ID = "farm_demo";
